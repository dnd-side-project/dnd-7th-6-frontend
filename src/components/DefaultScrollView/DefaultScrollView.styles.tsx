import styled from '@emotion/native';

import theme from 'src/styles/Theme';

export const DefaultScrollView = styled.View({
  backgroundColor: theme.colors.grayscale[1],
  width: '100%',
  height: '100%',
});
